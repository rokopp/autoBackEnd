package c_theory.question12;

public class Security {

    //todo A
    // What is authentication?
    // Answer: process of validating identity.

    //todo B
    // What is authorization?
    // Answer: Authorization is a security mechanism to determine access levels or user/client privileges.

    //todo C
    // For web (HTTP backend) Spring Security is configured as a ... ? Explain it.
    // Think or read how Spring Security checks are injection into your application.
    // Configured as (one word):
    // Description: it is configured as which role has access to which requests.

}
